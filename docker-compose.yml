version: '2.2'

services:

  vpn:
    image: vpn
    build: ./vpn
    privileged: true
    ports:
      - 1194:1194/udp
      - 443:443/tcp 

    nginx:
        image: vpn
        build: ./vpn
        privileged: true
        ports:
            - 1194:1194/udp
            - 443:443/tcp 

